/**
 * 学员端（Main / Learn / BottomNav）平板 md+ 主栏宽度一致。
 * 根页面自行负责 padding（如 px-4）；此处仅限宽 + 水平居中。
 */
export const LEARNER_MAIN_COLUMN_CLASS = 'w-full max-w-4xl mx-auto min-h-0'

/** 顶栏 / 底栏内层：与常见 px-4 内容对齐，md+ 略增水平内边距 */
export const LEARNER_CHROME_INNER_CLASS = 'w-full max-w-4xl mx-auto px-4 md:px-6'
